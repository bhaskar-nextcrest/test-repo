# OpenClaw/MoltBot Integration Guide for Aptness - Automate your Workflows seamlessly

> Auto-generated by Aptness Website Analyzer
> Website: https://aptness.ai/
> Generated: 2026-05-18T06:17:42.745Z


---

## About Your Site

**Aptness - Automate your Workflows seamlessly** — Aptness is an AI-powered automation platform that enables users to connect various applications, automate tasks, and streamline operations. It offers a marketplace for pre-built automation packages, tools to design custom AI agents, and a rapid agent feature for instant task execution. Additionally, it fosters a community where users can share their self-built automation systems, earn credits, and collectively reduce the cost of automation.

| | |
|---|---|
| **Industry** | Workflow Automation, Artificial Intelligence, Software as a Service (SaaS) |
| **Key Services** | AI-powered workflow automation platform, Automation Marketplace (pre-built packages), Custom AI Agent Builder, Rapid Agent for instant task automation, Application integration and connectivity, Community-driven system sharing and monetization, Analytics and reporting for automations |
| **Target Users** | Individuals seeking to automate personal or business tasks, Businesses looking to streamline operations, Creators and builders of automation workflows, Developers looking to share and monetize their automation expertise, Community members seeking cost-effective automation solutions, Sales and Marketing professionals (e.g., Welcome Email Sequence, Lead Capture to CRM), E-commerce businesses (e.g., Daily Sales Report) |
| **Tech Stack** | Next.js (frontend framework), React (inferred from Next.js), Cloud-based infrastructure (for scalability and AI agent execution), API integration framework, Database for storing user data, agent configurations, and automation run logs, AI/ML models for rapid agent and custom agent intelligence, Payment/credit system for monetization features |

---

## What OpenClaw/MoltBot Can Do On Your Site

### User Use Cases

#### 1. Find and Learn About Automation Packages
A user wants to understand what automation packages are available on Aptness.ai for a specific use case or industry without navigating manually.

**Actions:** User asks the bot to 'Show me automation packages for marketing'. -> Bot navigates to the Marketplace section and describes relevant examples.
**AI Help:** The AI can quickly direct the user to the relevant section and summarize package types, saving manual scrolling and reading.

#### 2. Initiate a Rapid Agent Task
A user has a simple task they want to automate instantly using Aptness's Rapid Agent feature, and wants to start the process.

**Actions:** User tells the bot 'I want to generate a weekly report from my Google Sheet using the Rapid Agent'. -> Bot fills the rapid agent input field with the user's task description.
**AI Help:** The AI streamlines the initial input for the Rapid Agent, allowing the user to express their automation need conversationally.

#### 3. Discover Integrated Applications
A user needs to know which applications Aptness can connect with to determine if it fits their existing tech stack.

**Actions:** User asks the bot 'What apps can Aptness connect to?' -> Bot lists the integrated applications displayed on the homepage.
**AI Help:** The AI quickly extracts and presents the list of supported applications, saving the user from having to visually scan the page.

#### 4. Understand How Aptness Works
A new user wants a quick overview of the Aptness platform's core steps from setup to running automations.

**Actions:** User asks the bot 'How does Aptness work?' -> Bot navigates to the 'How it works' section and provides a summary of the 3 steps.
**AI Help:** The AI provides an immediate, concise explanation of the platform's mechanics, guiding the user to essential information efficiently.

### Bot Use Cases

#### 1. Marketplace Content Monitoring
An autonomous bot periodically monitors the Aptness.ai homepage for updates or new prominent packages mentioned in the 'Marketplace' section.

**Data Retrieved:** Package names and descriptions from the 'Marketplace' section, Any new calls to action related to marketplace packages
**Automations:** Navigate to the 'Marketplace' section, Extract text content related to packages, Log changes or new package mentions to an external database
**API Interactions:** None (primarily UI interaction for public content)

#### 2. Integrated Apps List Compilation
A bot compiles a current list of all applications Aptness integrates with directly from the website's 'Connect your apps' section.

**Data Retrieved:** Names of integrated applications (e.g., Gmail, Sheets, GitHub)
**Automations:** Navigate to the 'Connect your apps' section, Extract all listed application names, Format the list for reporting or storage
**API Interactions:** None (primarily UI interaction for public content)

#### 3. Lead Generation Flow Initiation
An autonomous bot initiates the 'Get started free' flow to simulate a new user signup and test the initial user journey.

**Data Retrieved:** Status of the 'Get started free' button or link
**Automations:** Click the 'Get started free' button in the header, Monitor the subsequent page load or URL change
**API Interactions:** None (primarily UI interaction for public content)

#### 4. Promotional Content Discovery
A bot scans the Aptness.ai landing page to identify and log key promotional messages, new features highlighted, or community benefits.

**Data Retrieved:** Headings and paragraphs in sections like 'AI agents that get your work done', 'Share your magic', 'Everything in the box', Calls to action text
**Automations:** Navigate through various sections of the homepage, Extract and categorize prominent text elements, Store discovered content for analysis
**API Interactions:** None (primarily UI interaction for public content)

---

## Step-by-Step Setup

### Step 1: Set up MoltBot/OpenClaw Environment

Ensure you have Node.js (v18+) and npm installed. Install the MoltBot CLI globally.

```bash
npm install -g moltbot
```



### Step 2: Create Agent Directory

Create a new directory for your Aptness.ai agent and navigate into it.

```bash
mkdir aptness-agent && cd aptness-agent
```



### Step 3: Create `manifest.json`

Create a file named `manifest.json` in your `aptness-agent` directory with the provided content.


```js
{
  "name": "Aptness.ai Agent",
  "description": "An OpenClaw agent for interacting with the Aptness.ai website, helping users explore features and initiate workflows.",
  "version": "0.1.0",
  "skills": ["skills.md"],
  "homepage": "https://aptness.ai/",
  "auth": {
    "type": "none"
  },
  "browserSettings": {
    "startUrl": "https://aptness.ai/"
  }
}
```

> **File:** `aptness-agent/manifest.json`

### Step 4: Create `skills.md`

Create a file named `skills.md` in your `aptness-agent` directory with the provided content.


```js
### navigate_to_section(section_name: string)
Navigates the agent to a specific section on the Aptness.ai homepage by scrolling to the relevant heading. Valid sections include "Marketplace", "Custom Agents", "Rapid Agent", "How it works", "Share your magic", "Everything in the box".

### describe_task_to_rapid_agent(task_description: string)
Fills the text area on the homepage designed for the 'Rapid Agent' feature with a specified task description. This simulates expressing a task to the AI for execution.

### list_integrated_apps()
Retrieves a list of applications that Aptness integrates with, as displayed in the 'Connect your apps' section of the homepage.

### start_getting_started_flow()
Clicks the 'Get started free' button typically found in the header, initiating the user onboarding or signup process.
```

> **File:** `aptness-agent/skills.md`

### Step 5: Create `skills.js`

Create a file named `skills.js` in your `aptness-agent` directory with the provided JavaScript implementation.


```js
export default class AptnessSkills {
  constructor(agent) {
    this.agent = agent;
  }

  async navigate_to_section(section_name) {
    await this.agent.goto("https://aptness.ai/"); // Ensure we are on the homepage first
    const lowerSectionName = section_name.toLowerCase();
    if (lowerSectionName === "marketplace") {
      await this.agent.scrollTo('h3:has-text("Marketplace")');
    } else if (lowerSectionName === "custom agents") {
      await this.agent.scrollTo('h3:has-text("Custom Agents")');
    } else if (lowerSectionName === "rapid agent") {
      await this.agent.scrollTo('h3:has-text("Rapid Agent")');
    } else if (lowerSectionName === "how it works") {
      await this.agent.scrollTo('h2:has-text("From zero to automated in 3 steps")');
    } else if (lowerSectionName === "share your magic") {
      await this.agent.scrollTo('h2:has-text("Share your system. Share your magic.")');
    } else if (lowerSectionName === "everything in the box") {
      await this.agent.scrollTo('h2:has-text("More than just agents — a complete platform")');
    } else {
      throw new Error(`Unknown section: ${section_name}. Valid options are: Marketplace, Custom Agents, Rapid Agent, How it works, Share your magic, Everything in the box.`);
    }
    return `Navigated to the '${section_name}' section.`;
  }

  async describe_task_to_rapid_agent(task_description) {
    await this.agent.goto("https://aptness.ai/");
    // The rapid agent input is a textarea with a specific placeholder
    const inputSelector = 'textarea[placeholder*="Generate a weekly report from my sheet"]';
    const inputElement = await this.agent.page.locator(inputSelector).first();
    if (!await inputElement.isVisible()) {
      await this.agent.scrollTo(inputSelector);
    }
    await this.agent.fill(inputSelector, task_description);
    // There isn't an explicit 'Run' button on the landing page associated with this input.
    // The text 'Or try your own task' is a button. Clicking it may lead to the actual Rapid Agent interface.
    const tryOwnTaskButton = await this.agent.page.locator('button:has-text("Try your own task")');
    if (await tryOwnTaskButton.isVisible()) {
      await tryOwnTaskButton.click();
      return `Filled the Rapid Agent input with: "${task_description}". Initiated the 'Try your own task' flow.`;
    } else {
      return `Filled the Rapid Agent input with: "${task_description}". No explicit submission button found on this page.`;
    }
  }

  async list_integrated_apps() {
    await this.agent.goto("https://aptness.ai/");
    // Scroll to the 'Connect your apps' section
    await this.agent.scrollTo('h2:has-text("Connect your apps")');

    const apps = await this.agent.evaluate(() => {
      const appNames = [];
      // Select elements containing app names, typically <p> tags within specific containers
      const appElements = document.querySelectorAll('div.flex.flex-col.items-center.gap-2 p.text-sm.font-medium');
      appElements.forEach(el => {
        appNames.push(el.textContent.trim());
      });
      return appNames;
    });
    return `Aptness integrates with the following apps: ${apps.join(', ')}.`;
  }

  async start_getting_started_flow() {
    await this.agent.goto("https://aptness.ai/");
    // Click the 'Get started free' button in the header navigation
    const getStartedButton = await this.agent.page.locator('button:has-text("Get started free")');
    if (await getStartedButton.isVisible()) {
      await getStartedButton.click();
      return "Initiated the 'Get started free' flow. You should now be on the signup/onboarding page.";
    } else {
      return "'Get started free' button not found.";
    }
  }
}
```

> **File:** `aptness-agent/skills.js`

### Step 6: Run the Agent Locally

Start the MoltBot agent in developer mode to test its functionality.

```bash
moltbot dev
```



### Step 7: Deploy the Agent (Optional)

Once satisfied, deploy your agent to a hosting platform like MoltBot Cloud or Vercel to get a public URL.

```bash
moltbot deploy
```



---

## UI Integration (Chat Widget)

# MoltBot/OpenClaw Widget Integration Guide

To embed the MoltBot/OpenClaw chat widget on your Aptness.ai website, follow these simple steps.

## Step 1: Obtain Your Agent URL

Before you begin, ensure your MoltBot agent for Aptness.ai is deployed and you have its public URL. If you're running it locally, the URL might be `http://localhost:8000` (check your MoltBot CLI output).

## Step 2: Add the Embed Code to Your Website

Locate the HTML file(s) where you want the chat widget to appear. This is typically in your main layout file (e.g., `_app.js` or `layout.js` if using Next.js/React, or `index.html` for a static site).

Paste the following embed code just before the closing `</body>` tag of your HTML document:

```html
<script type="module" src="https://unpkg.com/moltbot?ui=true"></script>
<molt-bot agent-url="YOUR_AGENT_URL"></molt-bot>
```

**Important:** Replace `YOUR_AGENT_URL` with the actual URL of your deployed Aptness.ai MoltBot agent.

## Step 3: Customize (Optional)

The `molt-bot` web component offers several attributes for customization:

*   `title`: Title displayed in the chat widget header.
*   `placeholder`: Placeholder text for the input field.
*   `initial-message`: An initial message from the bot when the chat opens.
*   `theme`: 'light' or 'dark' (if supported by MoltBot UI).

Example with customizations:

```html
<molt-bot
  agent-url="YOUR_AGENT_URL"
  title="Aptness AI Assistant"
  placeholder="Ask me about Aptness..."
  initial-message="Hello! How can I help you explore Aptness.ai today?"
></molt-bot>
```

## Step 4: Test the Integration

Save your changes and open your website in a browser. You should see the MoltBot chat widget (usually a small icon) at the bottom right corner of your page. Click on it to open the chat interface and start interacting with your Aptness.ai agent.

### Widget Embed Code

Paste this before `</body>` in your main HTML file:

```html
<script type="module" src="https://unpkg.com/moltbot?ui=true"></script>
<molt-bot agent-url="YOUR_AGENT_URL"></molt-bot>
```

---

## Bot-Friendliness Audit

| Item | Status | Recommendation |
|------|--------|----------------|
| Clear HTML Structure and Semantic Elements | Good | The website uses standard HTML5 elements and a reasonably clear structure, making it straightforward for bots to parse content. |
| Stable Element IDs/Classes for Key Interactions | Needs Work | Many interactive elements rely on generic class names or text content for identification, which can be brittle. Adding unique and stable IDs to critical elements (e.g., input fields, buttons for main actions) would improve bot reliability. |
| Robots.txt Configuration | Good | Aptness.ai has a robots.txt file that allows crawling (`User-agent: *
Allow: /`), indicating bot-friendly access. |
| Accessibility Features (ARIA attributes, semantic tags) | Good | While not exhaustively checked, the use of semantic HTML and clear text elements generally aids accessibility, which is beneficial for bots that interpret page structure. |
| API Availability for Core Business Logic | Missing | The landing page describes API integrations for *external* services, but does not expose public APIs for managing Aptness accounts, creating agents, or interacting with the marketplace programmatically from *outside* the UI. This limits deeper autonomous bot use cases without UI automation or specific platform integrations. |

---

## Files To Create

| # | File | Path | Purpose |
|---|------|------|---------|
| 1 | `manifest.json` | `./aptness-agent` | The manifest file for the Aptness.ai MoltBot agent, defining its basic properties and starting URL. |
| 2 | `skills.js` | `./aptness-agent` | JavaScript implementation of the MoltBot agent's skills for interacting with Aptness.ai. |

---

## Automation Opportunities

- Generating weekly reports from spreadsheets
- Implementing welcome email sequences
- Creating daily sales reports
- Capturing leads and adding them to CRM systems
- Connecting various business applications (e.g., Gmail, Sheets, Drive, Calendar, GitHub, Slack)
- Triggering actions based on events (e.g., new subscriber)
- Logging data to spreadsheets
- Sending automated emails
- Managing and running predefined or custom workflows

## API Integration Opportunities

- Gmail API
- Google Sheets API
- Google Drive API
- Google Calendar API
- GitHub API
- Slack API
- CRM APIs (e.g., Salesforce, HubSpot)
- Marketing Automation Platform APIs (e.g., Mailchimp, SendGrid)
- E-commerce Platform APIs (e.g., Shopify, WooCommerce)
- Other productivity and business application APIs
